Steps to setup the starter template

1. Clone the project
git clone https://github.com/Vishwas-Dev/Express-TypeScript-starter-project.git
<ProjectName>


3. Move in to the folder structure
cd
<ProjectName>

5. Install npm dependencies
npm i

6. Create a new .env file in the root directory and add the PORT env variable
echo PORT=3000 >> .env

5.Start the express server
npm run dev
